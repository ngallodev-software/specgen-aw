# Phase PHASE-003 implementation prompt

Verify compatibility, migrations, boundaries, package installation, and independent review.

Execute only the declared tasks and dependencies in `pack.yaml`.

## Task order / dependencies
- `TASK-005` depends on `TASK-003`, `TASK-004`.

Agent-Workflow owns execution, worktrees, review, completion, and acceptance state.
